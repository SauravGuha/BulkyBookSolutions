@model Feature

<div class="row pb-3">

    <form method="post" asp-controller="Feature" asp-action="AddUpdate">

        <div class="row p-3">
            <input type="hidden" asp-for="Id" />
            <span asp-validation-for="ControllerName" class="text-danger"></span>
            <label asp-for="ControllerName"></label>
            <input asp-for="ControllerName" />
        </div>

        <div class="row p-3">
            <button type="submit" class="btn btn-primary col-6">
                @if (Model.Id == 0)
                {
                    <i>Add</i>
                }
                else
                {
                    <i>Update</i>
                }
            </button>

            <a class="btn btn-primary col-6" asp-action="Index">Back</a>
        </div>

    </form>

</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial"/>
    }
}